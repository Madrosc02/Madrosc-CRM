// PWA registration disabled — service worker was causing stale cache issues.
// This file is kept as a no-op so existing imports don't break.
console.log('ℹ️ PWA service worker is disabled to prevent stale cache issues.');
